swarm:
  datasources:
    data-sources:
      MyDS:
        driver-name: h2
        connection-url: jdbc:h2:mem:insurance;DB_CLOSE_DELAY=-1;INIT=CREATE TABLE INSURANCE (ID SERIAL PRIMARY KEY NOT NULL, orderId INT, state VARCHAR(50), accountId VARCHAR(100), name VARCHAR(100), totalCost NUMERIC);
        user-name: sa
        password: sa
